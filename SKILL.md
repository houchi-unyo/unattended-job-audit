---
name: unattended-job-audit
description: 定期実行のジョブ(launchd・cron・GitHub Actions)を棚卸しして、失敗が人に届くか・二重に走らないか・出力が古くなったら気づけるかを点検する。無人で動かしている仕組みが「黙って壊れている」状態を見つけたいときに使う。
---

# 無人ジョブの点検

自動で動く仕組みは、**動かなくなったことに気づけるかどうか**で価値が決まる。
このスキルは、いま登録されている定期ジョブを一覧にして、次の 10 点を機械で当たれる範囲まで当てる。

## 使い方

1. `scripts/scan_jobs.py` を走らせて一覧を作る。

   ```
   python3 scripts/scan_jobs.py            # launchd + cron + カレントの .github/workflows
   python3 scripts/scan_jobs.py --repo ~/src/myapp --json
   ```

   出力は「ジョブ名 / いつ走るか / 実行するもの / ログの行き先 / 引っかかった点」の表。

2. 引っかかった点を `reference/checklist.md` の番号と突き合わせ、直す順を決める。
   **10 件全部を直そうとしない。** 1・3・4 の 3 つが通っていれば、残りは後から足せる。

3. 直したあとに、もう一度走らせて差分を見る。

## 点検する 10 点

| # | 問い | 機械で当たるか |
|---|---|---|
| 1 | 失敗したとき、人に届くか | 実行するスクリプトに通知の呼び出しがあるか |
| 2 | 成功したときは黙っているか | 通知が無条件に走っていないか |
| 3 | 同じ仕事を二度しないか | 台帳・ロック・冪等キーの痕跡があるか |
| 4 | 出力が古くなったら気づけるか | 別の見張りが出力の鮮度を見ているか(人の目で確認) |
| 5 | 登録されている一覧と、あるべき一覧が一致するか | 人の目で確認(`expected_jobs.txt` のような一覧を置いて突き合わせる) |
| 6 | 秘密が環境変数・権限 600 のファイルにあるか | ログとスクリプトに秘密の直書きが無いか |
| 7 | 失敗したときに止まるか、暴走するか | `set -e` の有無・リトライの上限 |
| 8 | 自動でできなかった分を、人が拾える形で残すか | 失敗時の出力先(人が読む場所) |
| 9 | ログが残り、いつのものか分かるか | 出力先の指定があるか |
| 10 | 端末が寝ていたときの前提が書いてあるか | 人の目で確認 |

1〜3・6・7・9 は script が当たる。4・5・8・10 は人が見る。

## 直す順

**1(失敗が届く)→ 3(二重に走らない)→ 4(古くなったら気づく)** の順で入れる。
この 3 つが無い自動化は、動いているように見えて、止まった日から誰も気づかない。

通知は「失敗したときだけ」にする。毎回届く通知は 2 週間で読まれなくなり、
本当の失敗が埋もれる。成功は静かに、失敗だけ 1 行。

## 注意

- このスキルは**読むだけ**で、ジョブの登録・解除・書き換えはしない。直すのは人が決めてから
- **判定は文字列の手がかりなので、外れることがある。**たとえば `|| true`(失敗を握りつぶす書き方)も「失敗を見ている痕跡」に数えてしまう。引っかからなかった行も安心しきらない
- `launchctl list` の 2 列目が直近の終了コード。`0` 以外が残っていたら、そのジョブは最後に失敗している
- ジョブを足したり外したりしたら、あるべき一覧(#5)も同時に直す。片方だけ直すと点検が鳴り続ける

詳しい背景と、実際に起きた壊れ方は `reference/checklist.md` に書いた。
